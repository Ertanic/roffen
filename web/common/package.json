{
  "name": "common",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  },
  "scripts": {
    "build": "bun build --outdir out ./index.ts",
    "build-prod": "bun build --outdir out ./index.ts --production"
  }
}
